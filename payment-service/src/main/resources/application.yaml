server:
  port: ${APP_PORT_PAYMENT:8081}

spring:
  application:
    name: payment-service

  main:
    banner-mode: off

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/db.changelog-master.xml
    default-schema: public
    user: ${spring.r2dbc.username}
    password: ${spring.r2dbc.password}
    url: jdbc:postgresql://${DB_HOST_PAYMENT:localhost}:${DB_PORT_PAYMENT:5432}/${DB_NAME_PAYMENT:payment_repository}
    driver-class-name: org.postgresql.Driver

  r2dbc:
    url: r2dbc:postgresql://${DB_HOST_PAYMENT:localhost}:${DB_PORT_PAYMENT:5432}/${DB_NAME_PAYMENT:payment_repository}
    username: ${DB_USER_PAYMENT:postgres}
    password: ${DB_PASS_PAYMENT:postgres}
    pool:
      enabled: true
      initial-size: 10
      max-size: 50
      max-idle-time: 30s
      validation-query: SELECT 1
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/market-payment
          jwk-set-uri: http://keycloak:8080/realms/market-payment/protocol/openid-connect/certs


logging:
  level:
    root: WARN
    com.alex.market.payment: INFO
    com.alex.market.payment.service: DEBUG
    com.alex.market.payment.repository: INFO
    org.springframework.r2dbc: WARN
    org.springframework.transaction: INFO
  file:
    name: ${DIR_PAYMENT:/home/alexlakers/my-payment}/payment.log
  pattern:
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
    console: "%d{HH:mm:ss} %-5level %logger{36} - %msg%n"