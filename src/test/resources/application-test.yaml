spring:
  jpa:
    hibernate:
      ddl-auto: validate
  sql:
    init:
      mode: always
  liquibase:
    contexts: test
    enabled: true
    change-log: classpath:/db/changelog/db.changelog-master.xml