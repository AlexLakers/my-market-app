# my-market-app
Это  мультимодульный проект, который состоит из двух модулей: реактивное MVC-приложение(WebFlux+R2DBC), реактивный RESTfull сервис для обработки платежей.

## Модуль market-mvc
- В данном проекте реализована концепция 'MVC', но построенная на базе реактивной модели  с ипользованием реактивного веб-стека 'WebFlux' и реактивного доступа к данным 'R2DBC'.
- Cоответственно присутствует как frontend-часть на базе шаблонизатора Thymeleaf, так и backend-часть.
- Это приложение позволяет пользователю просматривать товары из каталога на базе гибкого динамического поиска.
- Далее, если товар заинтересовал - можно положить его в корзину в нужном количестве, после чего, например, оформить заказ на покупку через взаимодействие с сервисом платежей 'payment-service'.
- Также присутствует функционал для добавления новго товара в каталог с его изображением.
- Для взаимодействия с сервисом платежей 'payment-service' был сгенерирован http client по написанной Open Api спецификациии.
- Для уменьшения издержек на частые запросы в БД , часто используемые данные-товары были закешированы в  сторонней системе 'Redis'.


1. #### Модуль работы с товарами
Используются шаблоны страниц: item,items,newItem,newImage.
- ```GET  /?search=[search]&sort=[sort]&pageNumber=[pageNumber]&pageSize=[pageSize]``` - для динамического поиска товаров
- ```GET  /items?search=[search]&sort=[sort]&pageNumber=[pageNumber]&pageSize=[pageSize]``` - аналогично(алиас).
- ```POST /items?id=[id]&search=[search]&sort=[sort]&pageNumber=[pageNumber]&pageSize=[pageSize]&action=[action]``` - для изменения кол-ва в корзине(сл страницы товаров)
- ```POST /items/{id}?action=[action]``` - для получения товара.
- ```POST /items/{id}?action=[action]``` - для изменения кол-ва в корзине(со страницы товара)
- ```GET  /items/new``` - для показа формы добавления товара(деталей).
- ```POST /items/new``` - действие добавления товара(деталей).
- ```GET  /items/images/new``` - для показа формы добавления картинки для товара.

2. #### Модуль работы с корзиной
Для сохранения состояния корзины между запросами используется сессия, страница шаблона - cart.html
- ```GET /cart/items``` - для получения товаров в корзине и проверки баланса на счете через сервис платежей 'payment-service'.
- ```POST /cart/items?id=[id]&action=[action]``` - для изменения кол-ва в корзине(со страницы корзины).

3. #### Модуль работы с заказами
Тут используется имитация оформления заказа - показ страницы заказа, шаблоны страниц: order.html, orders.html
- ```GET /orders``` - для получения всех заказов
- ```GET /orders/{id}?newOrder=[newOrder]``` - для получения заказа
- ```POST /buy``` - для оформления(создания) заказа через взаимодействие в сервисом платежей 'payment-service'.

#### Рассмотрим пример:
У нас есть уже три товара в каталоге(с картинками),но мы решили добавить новый товар ```phone```.

Намеренно введем отрицательную стоимость товара, в результате на форме увидим ошибки валидации при сохранении введенных данных.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/item_add_incor1.png?raw=true)

Также в проекте были созданы страницы для других ошибок для их визцализации - 404.html, 400.html, 500.html

После этого товар добавится и будет предложено добавить для него картинку(опциональный шаг), мы добавим.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/image_ad.png?raw=true)

Затем после успешного добавления картинки для товара, которая будет сохранена на сервере в указанном хранилище посмотрим общий каталог товаров.

Также для более удобного динамического поиска можно ввести символы из названия товара или из описания.Мы покажем все товары.

Можно также выбрать сортировку по цене или алфавиту. Мы веберем по цене.

Что касается пагинации , то этот функционал тут тоже есть, сделаем отображение по 5 товаров

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/start_page_sort.png?raw=true)

Видим что товар ```phone``` добавлен  с картинкой, которую мы загрузили. Сработала пагинация и сортировка.

Хорошо, далее если пользователю понравился товар и цена, то он может добавить товары в корзину в нужном количестве.
Мы добавим один мяч и один телефон.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/cart.png?raw=true)

Ну и если пользователь не передумал, он оформляет заказ, взаимодействуя с сервисом платежей 'payment-service. 
Если на счете достаточно средств и сервис доступен, то проходит оплата и оформляется заказ, а корзина очищается.
Также он может удалить товар из корзины.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/order.png?raw=true)

#### ⚠️ Взаимодействие с сервисом платежей 'payment-service'
Но что если при взаимодействии с сервисом платежей 'payment-service', на счете будет недостаточно средств для покупки товаров из корзины.

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/BadMoney.png?raw=true)

Мы видим информационное сообщение о недостатке средств. Но также может получиться что сервис платежей просто недоуступен
во время проверки баланса

![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/AccountServIsUnavailable.png?raw=true)

Или сервис платежей недоступен во время осуществления платежа.
![alt text](https://github.com/AlexLakers/ParserJsonCsvToXml/blob/master/WinFormsCsvJsonXml/App_Data/pictures/PayIsUnavble.png?raw=true)

## модуль payment-service
- В данном проекте реализована концепция 'RESTfull', построенная на базе реактивной модели с ипользованием реактивного веб-стека 'WebFlux' и реактивного доступа к данным 'R2DBC'.
- Данный сервис используется как сервер для запросов от клиента - 'market-mvc'.
- Это приложение позволяет получать баланс пользователя и проводить оплату через транзакцию в клиентском приложении 'market-mvc'.
- Серверная часть (контроллеры и Дто) сгенерирована по написанной Open Api спецификациии.


1. #### Модуль для доступа к счету пользователя
Используются шаблоны страниц: item,items,newItem,newImage.
- ```GET /api/payments/accounts/{accountId}``` - для получения информации о счете пользователя(баланса).

2. #### Модуль для доступа к сервису оплаты.
Для сохранения состояния корзины между запросами используется сессия, страница шаблона - cart.html
- ``` POST /api/payments/pay ``` - для оплаты , тоесть создается транзакция оплаты и если все ок, происодит списание со счета.


### Основные компоненты 

> - В качестве основного фреймворка был использован 'Spring Boot v3.5.8'.
> - В качестве БД я использовал  PostgreSQL V 17.0, для тестов 'testcontainers'.
> - Для тестирования взаимодействия с внешним сервисом использовал 'WireMock'.
> - В качестве системы сборки я использовал 'Maven'.
> - Для сборки всего проекта с инфраструтктурой использовал Docker 27.3.1.
> - Java 21.
> - В качестве встроенного сервера использовал Netty(embedded) на неблокирующей модели.
> - Для маппинга использовал Mapstruct framework.
> - Для валидации использовал Hibernate Validator(в составе 'spring-boot-starter-validation').
> - Для системы кеширования в 'market-mvc' был использован Redi, для тестов 'testcontainers'.
> - Для генерации клиентского кода(market-mvc) и серверного кода(payment-service) использовал 'Open Api'.

### Сборка 

Сборка осуществляется с помощью инфраструктуры Docker с использованием Docker compose.

- В корневой папке многомодульного проекта вы найдете файл для сборки:  ```compose.yaml```
  В нем описаны все службы(контейнеры), которые необходимы для работы приложения в целом.
- В папке каждого модуля располагаются файлы сборки образа:```market-mvc/Dockerfile```,```payment-service/Dockerfile```.
  Для сборки и запуска приложения(всех сервисов) просто запустите из корня проекта следующую команду.

  ```
  docker compose up -d
  ```

- В результате поднимятся 2 базы данных Postgres, сервис оплаты и приложение интернет-магазина, а также
система кеширвоания Redis во внетренней сети Docker.

### ⚠️ Важное примечание
- Обратите внимание , вам надо создать файл ```.env``` с перемнными, которые описаны в ```.env.example```

```
docker compose up -d
```

Для запуска тестов
```
./mvnw test
```




